import React from 'react';
import { Screen, EmptyState } from '@/components/ui';

export default function LearnScreen() {
  return (
    <Screen>
      <EmptyState icon="🧠" title="Learning Paths" description="Coming in a future phase" />
    </Screen>
  );
}
