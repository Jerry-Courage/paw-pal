/** Backward compatibility types for SceneRenderer */
export interface VRConceptNode {
  id: string
  title: string
  description?: string
  sketchfab_keyword?: string
  model_url?: string
  position?: [number, number, number]
}
