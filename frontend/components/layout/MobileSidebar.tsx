'use client'

// MobileSidebar is now replaced by the TopNav mobile drawer.
// This file is kept as a no-op to avoid import errors from any remaining references.
export default function MobileSidebar({ open, onClose }: { open: boolean; onClose: () => void }) {
  return null
}
